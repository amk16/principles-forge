---
title: <Dimension name>
type: principle
tags: [principle, <dimension-slug>]
---

# <Dimension name>

> [!info] Provenance
> Received canon `[prior-knowledge]` + measured evidence from [[YYYY-MM-DD — <session-slug>]]. Single-session claims stay `[medium]` until a second session confirms.

## Principles

<!--
  Anatomy of a rule (copy this shape):
  N. **Bold imperative rule.** One or two sentences of explanation — WHY it works and
     WHEN it applies. End with an inline confidence + provenance tag.
  Tag forms:
    `[high — prior-knowledge]`                     ← received canon, not measured by us
    `[medium — 1 session]` ← [[YYYY-MM-DD — slug]] ← held once, across ≥2 exemplars
    `[high — N sessions, confirmed <date>: <exemplar> did X]`  ← promoted, durable
  A pattern only earns a spot here after holding across 2+ sessions AND owner approval.
-->

1. **State the rule as an imperative, not an observation.** "Lead with the concrete number" beats "strong examples tend to use numbers" — a principle is something you *do*. `[high — prior-knowledge]`
2. _<Add real principles as /generate promotes them off the trends layer, or as the owner writes them in from received canon — always tagged.>_

## Specifics worth reusing

<!-- A closing section of concrete, measured values pulled from exemplars — the "steal this
     exact thing" list. Link out to [[now]] for anything that's this-season-only (perishable). -->

- _<e.g. exact phrasings, counts, or values measured from named exemplars>_
- Current season-specific flavor lives in [[now]] (perishable) — keep it out of this durable page.
