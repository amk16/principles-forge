---
title: Principles Vault — hub
type: index
tags: [index]
---

# Principles Vault

Evidence-bound reference for **<<DOMAIN>>**, maintained by **<<PERSONA_TITLE>>** (see operating manual, `CLAUDE.md`). Three layers: durable principles you consult when doing real work, perishable trends refreshed each session, immutable evidence underneath.

> **New here? Run `/start`.** It introduces the kit in plain language and walks you all the way to your first output. (It runs `/setup` then your first `/generate` for you.)

## Use when doing real work

Read in this order:
1. The relevant `principles/` pages (<<DIMENSIONS>>) — what works.
2. [[now]] — what it looks like right now. Principles win when they conflict.

## Refresh understanding

Run the `/generate` skill. It takes 4–6 exemplars you paste in, extracts the measurement-schema specifics, appends immutable evidence to `raw/sweeps/`, rewrites [[now]], and proposes principle promotions.

## Map

- Principles (durable): _<dimension wikilinks — filled by /setup>_
- Trends (perishable): [[now]]
- Evidence (immutable): _<session wikilinks appear here as /generate runs>_
- Registry: [[sources]] · Ledger: [[log]]

## State

- Sessions run: **0** (unconfigured — run `/setup`)
- Promotion candidates pending: —
